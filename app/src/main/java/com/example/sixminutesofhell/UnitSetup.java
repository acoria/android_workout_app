package com.example.sixminutesofhell;

/**
 * Created by vtewes on 09.01.2018.
 */

public class UnitSetup {

    /*
    class UnitSetup (jede Klasse Workout hat ein Setup (könnte auch Config sein))
        Liste mit folgenden Elementen: unitLengthType | length
        addType( unitLengthType, length );
        static getUnitLengthByType

        getLength{
            Workout.getUnitSetup().getLengthByType(  ).

          ________
          WorkoutBuilder anstatt UnitProvider, hat eine build-methode? / Collector?

     */
}
